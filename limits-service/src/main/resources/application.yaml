spring:
  application:
    name: limits-service
  config:
    import: optional:configserver:http://localhost:8081
  cloud:
    config:
      profile: qa
  profiles:
    active: qa
limits-service:
  minimum: 2
  maximum: 100
